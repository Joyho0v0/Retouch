目前项目所出现的问题背景如下所示：
在上一个 版本中（相关实验数据记录在V1/result/selected_k_total_summary.csv中）选取了32通道的模型在megvii中的测试精度是78%,为什么现在更改过后的模型（相关实验数据记录在result/selected_k_total_summary.csv中）测试出来的精度是75%，为什么泛化能力反而下降了。而且使用筛选过后的通道继续第二阶段训练时（也就是解冻backbone时）模型的性能并没有提升，反而由于模型已经收敛了导致直接触发patience早停（我不知道该现象是好还是坏），
目前我做出的猜测和方案（该猜想可能是错误的，如果错误了，请你否认我的观点并提出你的猜想，如果正确了，请你合理点评我的观点，最后你再提出一些你觉得可能的原因）如下：
1. 上一个版本由于backbone参数的随机初始化，造就了实验结果的偶然性，导致了假性的性能优秀。
2. 既然我筛选通道后的模型本质上已经收敛了，那我能否在现有的二阶段训练框架的基础上为训练集及验证集也分出二阶段，也就是先分出一部分数据集（当然该数据集的大小应当进行合理设置，使它能够支撑我训练出一个相对优秀的模型以方便后续提取到的通道的权威性）用于一开始的OriginalModel.pth和nmi_channel_selector.pkl的训练，当通道筛选结束后使用筛选到的32通道进行第一阶段的冻结backbone训练FC，然后再解冻backbone基于另一部分的数据集继续训练模型，从而克服筛选通道后的模型已经收敛的问题。
现在，项目代码运行在名为Retouch的conda虚拟环境之中，请你优化我的项目代码，我需要实现的效果是，使用我筛选出的通道进行模型训练时，我的模型在源域上的准确度能得到上升，同时在跨域实验时，跨域准确度也会比原模型（OriginalModel.pth）高。
我整个项目的整体意图便是对模型所使用的特征图通道进行筛选，筛选出其中与任务密切相关的通道，忽视一些冗余/无关的通道信息，使得模型不会轻易过拟合到训练集上，从而增强模型的跨域泛化能力。所以请你基于我整体的项目意图，为我针对性地进行项目代码设计。
请将你的问题分析以及解决方案（项目代码的修改变动）一一写入change.md之中
由于我的基础一般，为了保障代码的清晰明了，请你在编写代码时，不要使用复杂句式，如匿名函数或parse等