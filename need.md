首先当前终端里有一个代码在跑，请你在后续运行代码或验证程序时不要干扰到当前终端的运行，你可以重新创建一个终端进行后续实验。
现在的问题背景如下：
对于ChannelSelect.py中所使用的贪心选取方法，虽然这种方法也能成功选取出重要性较高的通道，但是可能会遇到一种情况比如说：前面已经验证过的通道索引1，5，7的组合以及单独的10号通道对模型的重要程度（nmi贡献度）并不高，所以贪心第三轮选取结束后就会把1，5，7这三个组合给抛弃，但是实际上1，5，7，10进行组合反而nmi会大幅上升，如果我使用ChannelSelect.py是不是就会遇到无法选到1，5，7，10的可能性（当然这种猜想可能是错的，请你分析我的代码看看是否会出现这种现象，如果不会请你给出充分的理由来否定我的观点，然后在explain.md中详细地为我逐行解释我的ChannelSelect.py中的代码）。


现在我想使用另一种通道选择方法，代码编写在RandomSelect.py,python解释器使用名为Retouch的虚拟环境，我每次从原始的1280个通道中随机选取128个通道索引，测试这128个通道在ali/test上的nmi有多少，进行死循环通道选取（循环的停止由我手动完成），如果当前的nmi值比存储好的最佳通道的nmi值高的话，则使用当前的128个通道的索引更新当前的最佳通道选择器（当然，第一次循环是没有通道选择器的，此时直接将当前选取的索引存储为首个最佳通道选择器即可）；如果没有比最佳通道的nmi值高的话，则直接跳过进行下一轮循环，每次循环后在终端输出当前循环的次数以及目前最佳的nmi值，从最佳nmi大于0.6955开始，每次更新通道选择器后，使用当前更新好的最佳通道选择器画出我所需要的三折线nmi图（具体代码请借鉴script/Re_nmi.py中的代码）。由于我的基础一般，为了代码的清晰明了，请你不要使用复杂句式，如匿名函数和parse等


